#include <iostream>
#include <ctime>

/*class Schrodenger {
public:
    char *mem;

    Schrodenger() {
        srand(time(NULL));

        if (rand() % 2 > 1) {
            mem = new char[1];
        } else {
            mem = nullptr;
        }
    }

    void measure() {
        delete mem;
    }
};

int main(void) {
    Schrodenger cat;

    cat.measure();

    return 0;
};*/